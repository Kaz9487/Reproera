# Security policy

Reproera is currently an alpha release and receives security fixes on the
latest revision only.

Do not include credentials, private server names, home-directory paths, or
access tokens in a public issue. Report suspected command injection, archive
traversal, checksum bypass, or unsafe shell-initialization behavior privately
to the repository owner after the public repository is created.
