#!/usr/bin/env bash
case "${1:-}" in
    --version)
        printf 'gcc (GCC) @VERSION@\n'
        ;;
    -dumpfullversion|-dumpversion)
        printf '@VERSION@\n'
        ;;
    *)
        exec "${REPROERA_TEST_REAL_CC:-cc}" "$@"
        ;;
esac
